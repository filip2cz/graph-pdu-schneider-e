options = {
    "size": (20, 10),  # velikost grafu
    "dpi": 80,  # velikost grafu pro export
    "smoothing": 10,  # jak moc se maji vyhlazovat data
    "secondary_smoothing": 0,  # jak moc se maji vyhlazovat data ale 2
    "title": None,  # nadpis grafu
    "font_size": 20,  # velikost pisma
    "normalize": False  # normalizovat data -> udelat aby to bylo 0-1
}
